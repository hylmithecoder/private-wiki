# /etc/logrotate.d/trpl3c-wiki
/var/log/trpl3c-wiki/*.log {
	weekly
	rotate 8
	compress
	delaycompress
	missingok
	notifempty
	# The daemon keeps the file open; truncate in place instead of moving it.
	copytruncate
}
